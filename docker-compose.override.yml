# ============================================================
# 本地调试覆盖配置
# Docker Compose 自动合并此文件，无需手动指定 -f
# ⚠️ 仅用于本地开发，上传服务器前请勿携带此文件
# ============================================================

services:
  server:
    environment:
      WECHAT_MOCK_MODE: "true"
